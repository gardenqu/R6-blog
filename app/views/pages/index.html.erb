<h1>Blog Pages</h1>

<%# <%= @pages.first.tile first time of erb tag %> 

<%@pages.each do |page| #used for conditionals and loops %>
    <p> <%= link_to page.title, page_path(page) %></p>
<%end%>


<%= link_to 'New Page', new_page_path %>
